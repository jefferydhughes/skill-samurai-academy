/**
 * Static location fallback data.
 * The app reads locations from the Supabase `franchise_locations` table.
 * Add entries here only if you need a local override or fallback.
 */
export const locations = [];
